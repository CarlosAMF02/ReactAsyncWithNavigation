export class User {
  constructor(name, email, telNumber, address, district, city, state) {
    this.name = name,
    this.email = email,
    this.telNumber = telNumber,
    this.address = address,
    this.district = district,
    this.city = city,
    this.state = state
  }
}